/**
 * Registration Layout
 */

export const metadata = {
  title: 'Patient Registration | HealUI Clinic',
  description: 'Complete your patient registration with our AI-powered assistant',
};

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
