/**
 * Registration Complete Layout
 */

export const metadata = {
  title: 'Registration Complete | HealUI Clinic',
  description: 'Your registration is complete',
};

export default function RegistrationCompleteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
